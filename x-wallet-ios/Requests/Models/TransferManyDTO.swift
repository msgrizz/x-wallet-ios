//
// TransferManyDTO.swift
//
// Generated by swagger-codegen
// https://github.com/swagger-api/swagger-codegen
//

import Foundation



open class TransferManyDTO: Codable {

    public var senderId: Int64?
    public var receiverId: Int64?
    public var poolId: Int64?
    public var amount: Int?


    
    public init(senderId: Int64?, receiverId: Int64?, poolId: Int64?, amount: Int?) {
        self.senderId = senderId
        self.receiverId = receiverId
        self.poolId = poolId
        self.amount = amount
    }
    

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {

        var container = encoder.container(keyedBy: String.self)

        try container.encodeIfPresent(senderId, forKey: "senderId")
        try container.encodeIfPresent(receiverId, forKey: "receiverId")
        try container.encodeIfPresent(poolId, forKey: "poolId")
        try container.encodeIfPresent(amount, forKey: "amount")
    }

    // Decodable protocol methods

    public required init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: String.self)

        senderId = try container.decodeIfPresent(Int64.self, forKey: "senderId")
        receiverId = try container.decodeIfPresent(Int64.self, forKey: "receiverId")
        poolId = try container.decodeIfPresent(Int64.self, forKey: "poolId")
        amount = try container.decodeIfPresent(Int.self, forKey: "amount")
    }
}

